/*Programowanie proceduralne

grupa P00-33a

20.05.2019 Laboratorium 11

imie:

nazwisko:

indeks:

*/

/* Zadanie

Zdefiniuj strukture ComplexExp reprezentujaca liczbe zespolona w postaci
wyk³adniczej: Z = Mod(Z) * exp(i*Arg(Z)).

Napisz funkcje:
- pozwalajaca nadac wartosci zmiennej typu ComplexExp,
- wypisujaca liczbe zespolona w odpowiednim formacie na ekranie komputera,
- obliczajaca kwadrat liczby zespolonej, zgodnie z zasada: Z^2 = Mod(Z)^2 *exp(i*2*Arg(Z)),
- pozwalajaca pobrac wartosci zmiennej typu ComplexExp z pliku txt,
- zapisujacej dane okreslonej zmiennej typu ComplexExp do pliku.

Stworz projekt o w³aœciwym podziale kodu na 3 pliki:
- plik nag³ówkowy,
- plik Ÿród³owy dla napisanej funkcji oraz
- plik z kodem g³ównym, w ktorym umiescisz podany ponizej kod z funkcja main().

Zademonstruj dzia³anie wszystkich funkcji w pliku main.c.

Program bedzie oceniany tylko w przypadku,
gdy bedzie mozna go skompilowac i uruchomic!
----------------------------------------------------

*/